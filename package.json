{
  "name": "new_moon",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20.x",
    "npm": "10.x"
  },
  "scripts": {
    "dev": "cd knife-set && npm run dev",
    "build": "cd knife-set && npm install && npm run build",
    "start": "cd knife-set && npm start"
  }
}
