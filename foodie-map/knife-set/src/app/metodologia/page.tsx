export default function MetodologiaPage() {
  return (
    <main className="mx-auto max-w-7xl space-y-6 px-4 py-8 md:px-6">
      <section className="rounded-xl border border-border bg-card p-5">
        <h1 className="text-2xl font-semibold">Metodología KS</h1>
        <p className="mt-2 text-sm text-muted-foreground">
          KS considera que un local puede tener fortalezas y debilidades por línea de producto, momento de servicio,
          precio y estándar operativo. Por eso se evalúa en múltiples ejes y no solo por popularidad digital.
        </p>
      </section>
    </main>
  );
}
